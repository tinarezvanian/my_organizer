# Conda environment for my_organizer (Python 3.13)
# Create: conda env create -f env.yml
# Activate: conda activate org
name: org
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
    - pypdf>=4.0.0
